Jupyter lab notebook and Python3 source code for LEAF-Toolbox Site using Google Earth Engine Python API.

Note: This is produces LEAF-Toolbox maps with exactly the same algorithm as used for image products ()
using the GEE Javascript API () or Python Code ()

You will need an Anaconda environment configured as:

>Anaconda
>>conda create --name leaftoolbox
>>conda activate leaftoolbox 
>>conda install -c conda-forge jupyterlab -y
>>conda install -c conda-forge earthengine-api -y
>>conda install -c conda-forge pandas -y

Then run as follows:
>Anaconda
>>conda activate leaftoolbox
>>jupyter lab

Select the notebook Leaf-Toolbox-Sites.ipynb.

